#!/bin/bash


set -e
set -x


PROJECT_ENV=ci


docker rm $PROJECT_ENV || true
docker run \
    -e PROJECT_ENV=$PROJECT_ENV \
    -l $PROJECT_ENV \
    -v $PWD:/workspace \
    -w /workspace \
    --entrypoint /workspace/bin/entrypoint \
    --name $PROJECT_ENV \
    python:slim \
    $PROJECT_ENV
