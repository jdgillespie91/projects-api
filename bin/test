#!/bin/bash

set -e
set -x

docker run \
  -v $PWD:/workspace \
  -w /workspace \
  -it \
  python:3.6-alpine sh -c \
  "apk update && apk add build-base && pip install -e .[dev] && pytest && mypy --strict projects"

